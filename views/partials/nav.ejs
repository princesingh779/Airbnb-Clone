<header class="header">
  <div class="logo">
    <img src="/images/fonticon.png" alt="hello" />
  </div>

  <nav class="nav">
    <a href="/"> Home </a>
    <% if(isLoggedIn) { %> <%if(user.type === 'user'){ %>
    <a href="/bookings">Bookings</a>
    <a href="/favourites">Favorite</a>

    <% } else { %>
    <a href="/host-homes">Host Homes</a>
    <a href="/add-homes"> Add your home</a>
    <% } %> <% } %> <% if(!isLoggedIn) { %>
    <a href="/auth/login">Log in</a>
    <a href="/auth/signup">Sign up</a>
    <% } else { %>
    <form action="/auth/logout" method="post">
      <button class="logout-btn" type="submit">Logout</button>
    </form>

    <% } %>
  </nav>
</header>
